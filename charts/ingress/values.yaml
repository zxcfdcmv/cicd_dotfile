ingress-nginx:
  controller:
    image:
      registry: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/registry.k8s.io/ingress-nginx
      image: controller
      tag: v1.15.1
      digest: ""  # 关键：置空，避免 digest 覆盖 registry 和 tag
    admissionWebhooks:
      patch:
        image:
          registry: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/registry.k8s.io/ingress-nginx
          image: kube-webhook-certgen
          tag: v1.6.9
          digest: ""  # 同样置空
    service:
      type: NodePort
